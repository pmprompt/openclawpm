# Sprites / Fly
# Used by Sprites CLI / SDK for provisioning
SPRITES_TOKEN=

# Telegram (prototype channel)
TELEGRAM_BOT_TOKEN=
TELEGRAM_ALLOWED_CHAT_ID=

# Model provider (prototype)
# Prefer BYO-key later; for MVP you can use a test key.
# Set exactly one of these.
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
OPENROUTER_API_KEY=

# Optional: model override (defaults to whatever OpenClaw picks for the provider)
# e.g. sonnet
OPENCLAW_MODEL_PRIMARY=

# Optional: OpenClaw version to install (defaults to latest)
# Use "latest" for newest stable, "beta" for beta channel, or specific version like "2026.2.15"
OPENCLAW_VERSION=latest

# OpenClaw gateway auth (per-sprite)
# For automation, generate per sprite and pass into config.
OPENCLAW_GATEWAY_TOKEN=

# Git
PM_SKILLS_REPO=https://github.com/pmprompt/claude-plugin-product-management.git
PM_SKILLS_REF=main
